# OpenAI API Key (Bắt buộc - dùng cho Task 4/5 embedding text-embedding-3-small
# và Task 10 generation. Đây là LLM/embedding provider DUY NHẤT của dự án —
# không dùng OpenRouter, không có fallback provider khác)
OPENAI_API_KEY=sk-proj-...

# PageIndex API Key (Tùy chọn - dùng cho Task 8: Vectorless RAG fallback)
# PAGEINDEX_API_KEY=pix_...

# Task 7 Reranking: dùng model BAAI/bge-reranker-v2-m3 chạy LOCAL qua
# sentence-transformers — không cần API key.
